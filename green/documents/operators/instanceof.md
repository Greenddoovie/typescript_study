# instanceof

Checks if an object is an instance of a specified class or constructor function at **runtime**.

## Keyword

It is done during runtime.
The interface, types, type aliases are not working well in runtime.
The JS doesn't have those features which are additional to TS.

The additional features are removed during compilation.
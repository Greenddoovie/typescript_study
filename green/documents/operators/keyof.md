# keyof operator

It is used to get a union type of all keys in an object type.
It is one of type operators which means it must be with type.

So if we use keyof operator for an object, get like this;

```typescript
keyof typeof ObjectName
```
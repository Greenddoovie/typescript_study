# Value & Type difference

There are two world in the TS framework.
1. Value world
2. Type world

The type world have to be removed on compile because the plain JS can not have the type framework.
So the same name can be declared on each world in TS framework.
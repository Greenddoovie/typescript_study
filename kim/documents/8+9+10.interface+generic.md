# 1. interface vs type

### interface

확장 가능, 선언 병합 가능

### Type

Union, intersection

```tsx
//interface
interface {
    name: string;
    age: number;
}

//이렇게 바로 추가 가능(선언 병합)
interface {
    email: string;
}

//Type
type Animal = "Cat" | "Dog" | "Rabbit";



//type에서 병합

type Geeks = {
  name: string;
  age: number;
};

type MoreGeeks = {
  email: string;
};

type CombinedGeeks = Geeks & MoreGeeks;

const gfg: CombinedGeeks = {
  name: "kgowda",
  age: 20,
  email: "kgowda@gmail.com"
};
```

## 실전에서 많이 쓰는 패턴

```tsx
// API 응답 타입 정의
interface ApiResponse<T> {
  data: T;
  status: number;
  message: string;
}

interface Satisfaction {
  satisfied: number;
  unsatisfied: number;
  noresponse: number;
}

// 실제 사용
const response: ApiResponse<Satisfaction> = {
  data: { staisfied: 40, unsatisfied: 35, noresponse: 70 },
  status: 200,
  message: "Satisfacton Survey",
};
```

### Index Signature

인덱스 시그니처는 객체가 <key, vlaue>형식이며 key와 value의 타입을 정확하게 명시해야하는 경우 사용합니다

```tsx
interface BankRates {
  [bankName: string]: number; // 어떤 은행 이름도 키로 가능
}

const rates: BankRates = {
  KB국민은행: 3.5,
  신한은행: 3.7,
  우리은행: 3.6,
  // 은행 추가 자유롭게 가능
};
```

### template literal

key의 타입을 Template literal로 선언 하는 경우
키값으로 단순한 타입뿐만이 아니라 어느 값을 사용할 수 있을지 정해줄 수 있는 유용한 로직입니다

```tsx
type userInfoType = "name" | "age" | "address";

type userType = {

	**// in 키워드를 사용해야 합니다!**
  [key in userInfoType]: string;
};

let user: userType = {
  'name': '일론 머스크',
  'age': '38',
  'address': '라스베가스'
};
```

### 함수 타입 interface // interface with method signature

```tsx
interface Calculator {
  (height: number, weight: number): number;
}

const SubtractHW: Calculator = (height, weight) => {
  return height - weight;
};

// 또는 메서드 형태
interface Calculator {
  add(a: number, b: number): number;
  subtract(a: number, b: number): number;
}

class SimpleCalculator implements Calculator {
  add(a: number, b: number): number {
    return a + b;
  }

  subtract(a: number, b: number): number {
    return a - b;
  }
}

const calc = new SimpleCalculator();

console.log(calc.add(5, 3));
console.log(calc.subtract(9, 4));
```

### Extends

```tsx
interface baseEntity {
  id: number;
  createdAt: string;
}

interface User extends baseEntity {
  name: string;
  age: number;
}

interface Admin extends baseEntity {
  role: "admin" | "super-admin";
}
```
